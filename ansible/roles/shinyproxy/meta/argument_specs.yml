argument_specs:
  main:
    options:
      shinyproxy_version:
        type: str
        description: Version of the ShinyProxy to install
        default: "3.1.1"
      farmers_config_remote:
        type: str
        required: true
        description: Path to farmers config file on remote host
